# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
backend/.env

# Contains real user PII (auth rows, data import) — never commit
migration/

# Scratch drop zone for Danielle to hand off before/after condition photos —
# Claude reads these and uploads them to the service-photos Supabase bucket,
# they're never meant to live in the repo itself.
/photo-uploads/

# Python
__pycache__/
*.pyc
backend/.venv/
