/**
 * This page is retired (2026-07-15) — the describe-pair/anything-else/
 * added-to-bag flow that used to live at /start-repair/pick is now a popup
 * (PairFlowDialog.tsx, mounted in App.tsx), opened via
 * RepairFlowContext.openPairFlow() from the checklist, a service's "Add to
 * repair", or a package's "Start a repair" — no page or route needed.
 *
 * Left as a stub rather than deleted outright so removing this file is a
 * deliberate, separate decision rather than a side effect of this change.
 */

const StartRepairPick = null;

export default StartRepairPick;
