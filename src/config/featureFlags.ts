/**
 * Master switch for the public-facing site.
 *
 * When `COMING_SOON_MODE` is true, every route renders the Coming Soon page
 * and all non-root paths redirect to "/".
 *
 * Set this to `false` to bring the full site back online.
 */
export const COMING_SOON_MODE = true;
